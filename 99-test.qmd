
`01-basics.qmd` 可以放更多内容，然后在 `_quarto.yml` 的 `website.navbar` 里加上链接。

---

## 5. 使用步骤

1. 创建仓库，把上面的文件放进去。
2. 打开仓库 **Settings → Pages**。
3. 在 **Build and deployment** 中，Source 选择 **GitHub Actions**。
4. 推送代码到 `main` 分支。
5. 等待 Actions 运行完成。
6. 网页会发布到：
   ```text
   https://<你的用户名>.github.io/<仓库名>/
